//The isArray() method returns true if an object is an array, otherwise false.
// array ma che ke nahi te check karse
// hase to true ether false
const fruits=['banana','mango'];
const results=Array.isArray(fruits);
console.log(results);

const numbers="1,5,6,5,2,7";
const result=Array.isArray(numbers);
console.log(result);