//The sort() method sorts the elements of an array in place and returns the sorted array.
//he array elements are converted to strings, then sorted according to each character's Unicode code point value.
//abcd & 123.... sorted kare
const numbers=[1,2,3,5,4,6,7,8,9];
console.log(numbers.sort());

const numbers1=[1,52,79895,125956,3232,9582555];
console.log(numbers1.sort());

const months=['jan','feb','march','august'];
console.log(months.sort());
